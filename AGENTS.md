# Boom Project Agent Instructions

## 專案核心目標

Boom 的最終目標是：**讓飛行中的無人機可以即時偵測、顯示荒野煙霧，並估算疑似煙霧源的位置。**

煙霧分割、影像變化與光流分析，都是辨識荒野煙霧事件的技術手段。
規劃或修改功能時，應以荒野環境監測為主軸，考慮雲霧、粉塵與相機移動造成的干擾。

## 功能優先順序

依下列順序評估方案與修改：

1. 飛行期間能可靠偵測荒野煙霧事件。
2. 降低端到端延遲，讓結果可即時顯示。
3. 在影像上清楚標示煙霧區域、事件編號與狀態。
4. 結合無人機 GPS、高度、姿態與相機資訊，估算地面座標。
5. 適應機載設備有限的 CPU、GPU、記憶體與電力。
6. 保存事件時間、畫面、座標與信心資訊，供事後檢查。

## 技術方向

- 將離線影片流程視為演算法驗證環境，不是最終部署型態。
- 新功能應盡量能移植至 `artillery/online/` 的即時處理流程。
- 偵測器與執行環境保持分離，讓不同偵測方法可以共用離線或即時 runner。
- 評估偵測方法時，至少考慮誤報、漏報、偵測延遲、處理 FPS 與定位誤差。
- 無法確定是煙霧或其來源位置時，輸出候選事件與信心資訊，不要把推測當成確定結果。

## 工作原則

- 修改前先閱讀相關程式、`PROJECT_STRUCTURE.md` 與對應方法的 README。
- 優先做最小且可驗證的修改，不任意重構或刪除既有功能。
- 保留 `artillery/` 現有匯入路徑的相容性，除非使用者明確要求遷移。
- 不覆蓋使用者既有輸出、資料、模型或設定。
- 修改後提供執行指令，並驗證程式至少能載入或完成對應測試。

## 專案主軸的標準描述

需要說明本專案時，優先使用以下描述：

> Boom 是一套用於飛行中無人機的荒野煙霧偵測與定位系統，目標是在影像中即時標示煙霧區域，並估算疑似煙霧源的地面座標。
